import 'package:flutter/material.dart';


class FoodDrinkPub extends StatelessWidget {
  const FoodDrinkPub({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return CustomScrollView(
      slivers: [

        SliverToBoxAdapter(
          child: Container(
            child: Column(
              children: [


              ],
            ),
          ),
        )


      ],
    );
  }
}
